# Web Push (VAPID) public key used by the browser to subscribe to push.
# Generate a key pair with:  npx web-push generate-vapid-keys
# Put the PUBLIC key here; keep the PRIVATE key only in the Supabase secrets.
VITE_VAPID_PUBLIC_KEY=
