# Frontend dependencies are installed from the public npm registry.
# The Supabase Edge Functions under src/supabase/functions run on Deno and
# import their dependencies directly via `jsr:`/`npm:` specifiers, so no JSR
# registry configuration is required here.
